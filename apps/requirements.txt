# Shared dependencies for all three apps
requests>=2.31.0
urllib3>=2.0.0
certifi>=2024.0.0
charset-normalizer>=3.0.0
idna>=3.0

# Hotel Desk + Restaurant POS (Windows)
PyQt6>=6.6.0
pyinstaller>=6.0.0   # for building .exe

# Table Order (Android/tablet) — install only on mobile/Linux build target
# kivy>=2.3.0
# buildozer>=1.5.0   # for building .apk (Linux only)
